# Telegram bot username for deep links from the Mini App (no @) — example: MyCompanyBot
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=

# Server-side (same as backend) for /api/chat and /api/me
GROQ_API_KEY=
BOT_TOKEN=
